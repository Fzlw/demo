let amount = 3399,
    days = 10,
    min = 100,
    max = 1000;


let minCount = 0, // 最多还多少次
    maxCount = 0; // 最少还多少次

// 逻辑
maxCount = Math.floor(amount / min);
minCount = amount / max





// 结果
console.log(maxCount)
console.log(minCount)